{
  "private": true,
  "scripts": {
    "install": "cd expensync && npm ci",
    "build": "cd expensync && npm run build",
    "dev": "cd expensync && npm run dev",
    "start": "cd expensync && npm start"
  }
}
